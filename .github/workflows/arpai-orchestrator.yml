name: ARPAI Orchestrator (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This obsolete scaffold workflow is disabled. ARPAI-site now deploys through Vercel/GitHub integration."
